/* 정호중
제 생각은 이래요

1. 유저만 생성한다.(회원가입을한다.)
2. 로그인을 한다.
3. 로그인한 사용자가 보드 CRUD 가능 (보드가 게시글)
로그인하지 않은 사용자는 보드 전체 조회만 가능 (리스트 목록 등의 상세보기나, 생성 수정 삭제는 불가능)

4. 기능이 다 완성이 되면 그때 membership 뭐시기..어쩌고... 한다.


.env 상수 내용
DB_HOST=
DB_USER=
DB_PASS=
DB_NAME=00_trello
DB_DIALECT=mysql
*/

내일 PR 보낼 때 적을 커밋 메세지
1. 회원가입 에러처리 service 파일로 이동(테스트 완료)
2. /config/config.js와 .env를 사용함에 따라 config/config.json 파일 삭제
3. /models/index.js 파일에서 sequelize가 읽어오는 config 파일을 config.json에서 config.js로 변경(9번째 줄 변수 config)
4. 로그인 에러처리 service 파일로 이동(테스트 완료)